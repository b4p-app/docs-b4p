#include <fstream.h>void main(void) {    ofstream output("LETTERS.DAT");   for (char letter = 'A'; letter <= 'H'; letter++)     output << letter;   for (letter = 'I'; letter <= 'Z'; letter++)     output << letter;   output.close();  //the problem starts here:      fstream letters( "LETTERS.DAT", ios::in | ios::out );      letters.seekp(8, ios::beg);   for (char letter1 = 'I'; letter1 <= 'Z'; letter1++)     letters << letter1;     letters.seekg(0, ios::beg);   while (! letters.eof())     cout.put((char)letters.get());   letters.close();  return; }