/* CODE EXAMPLE #1 */#include <iostream.h>#include <fstream.h>#include <stdlib.h>int main(){	int				num;	char			text[20];	char 			buffer[256];	//line buffer	char			filename[30];	//file name	cout << "Enter name of input file ->";	cin  >> filename;	ifstream	STREAM;						STREAM.open (filename, ios::in);	STREAM.getline( buffer, sizeof(buffer),'!' );	cout<<"filepos: "<<STREAM.tellg()<<endl;	cout<<buffer<<endl;				ofstream	ofs("outfile");		//write only	for (char letter = 'A'; letter <= 'H'; letter++)		ofs << letter;	ofs.close();//	ifstream	ifs("infile");		//read only	//	ifs >> i >> j >> k;//	ifs.close();	fstream	iof;	iof.open("iofile");				//open read/write	iof << "123456789_abcdfghijk"<<1956;	iof.seekg(0L, ios::beg);			//move read pointer 									//to beginning of file	iof >> text >> num;	iof.close();	cout<< text << num<< endl;   fstream letters( "outfile", ios::in | ios::out );   letters.seekp(8L, ios::beg);   for (char let = 'I'; let <= 'Z'; let ++)		letters << let;     	letters.seekg(0L, ios::beg);		//rewind	while (! letters.eof())			//display to console	cout.put((char)letters.get());	letters.close();		return EXIT_SUCCESS;		//int main()} /* the problem starts here:   fstream letters( "outfile", ios::in | ios::out );   letters.seekp(8, ios::beg);   for (char letter = 'I'; letter <= 'Z'; letter1++)     letters << letter;   letters.seekg(0, ios::beg);   while (! letters.eof())     cout.put((char)letters.get());   letters.close();  */