#include <iostream.h>				// cin,cout#include <stdio.h>					// NR headers#include <stddef.h>#include <stdlib.h>#include <strstream.h>				//string streams#include <fstream.h>				//stream manager#include <ctype.h>					//isalpha, isspace#define  NRANSI						//***must have to load memory.cp***#include "memory.h"					//RMR-NRUTIL#include "memory.cp"				//RMR-NRUTIL#include "raf_util.cp"#include "raf_util.h"#include <math.h>					//double pow(double,double)	/*------------------------------------------------------------------*/			void toFloat( char *word ) 		{		int i=0, len=0, decimal=0, sign = 1;		float num;				if( word[0]=='-') sign= -1;				while( word[i] != '\0')			{			if( word[i]=='.') decimal=i;			i++;			}		len=i;				cout<<word<<endl;		cout<<"sign: "<<sign<<endl;			cout<<"length: "<<len<<endl;				cout<<"decimal index: "<<decimal<<endl;				if( decimal) cout<<"there is a decimal point"<<endl;		cout<<"pow(10,2)= "<<pow(10,2)<<endl;		cout<<"pow(10,-2)= "<<pow(10,-2)<<endl;			}/*------------------------------------------------------------------*/void main(void){	char	string[]="34/34		12:23:54 65  76";	int		words=0,i=0;// create char data array of appropriate size	char** array;	array = matrixChar(1, 100, 1 , 20);			cout<<string<<endl;	toWordArray(string,words,array);	cout<<endl<<"words: "<<words<<endl;		float value[20];	for(i=1;i<=words;i++)		{		if( (i % 4)==0 ) cout<<endl;		cout<<"["<<i<<"]"<< array[i]<<"\t";			//value[i]=atof( array[i]);			// "\tF["<<i<<"]"<< value[i]<<endl;		}				/*	float	x;	toFloat( string );	x=atof(string)/100;	cout<<"atof(string): "<< x <<endl;		//select columns to import	char	ans[40];	cout<<"Enter column list ==>";	cin>>ans;	toWordArray(ans,words);	cout<<endl<<"words: "<<words<<endl;*/			return;}			