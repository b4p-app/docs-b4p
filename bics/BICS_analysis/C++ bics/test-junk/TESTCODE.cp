#include <iostream.h>				// cin,cout#include <stdio.h>					// NR headers#include <stddef.h>#include <stdlib.h>#include <strstream.h>				//string streams#include <fstream.h>				//stream manager#include <ctype.h>					//isalpha, isspace#define  NRANSI						//***must have to load memory.cp***#include "memory.h"					//RMR-NRUTIL#include "memory.cp"				//RMR-NRUTIL#include "raf_util.cp"#include "raf_util.h"#include <math.h>					//double pow(double,double)/*------------------------------------------------------------------*/cout<<lineSeparator<<endl;INFILE.getline(line,sizeof(line) );cout<<"inline: "<<endl<<line;istrstream buffer( line );while( ! buffer.fail() )	{		buffer>>word;		cout<<word<<endl;	}cout<<lineSeparator<<endl;INFILE.getline(line,sizeof(line) );istrstream buffer2( line );int count2=0;for( char letter= buffer2.get();  !buffer2.eof(); letter=buffer2.get() )	{	count2++;	cout<<letter<<"*";	}cout<<"num chars: "<<count2<<endl;cout<<lineSeparator<<endl;INFILE.getline(line,sizeof(line));int charcount=0;charcount = stringlen(line);cout<<"charcount: "<<charcount<<endl;/*------------------------------------------------------------------*///select columns to import	cout<<"Enter column list ==>";	cin>>ans;	int commas=0;	i=0;	while( (char)ans[i] != '\0' ) 		{		if(  (char)ans[i] == ','  )  			{			commas++;			ans[i]=' ';			//replace commas -> space			}			 		i++;		}	int ncols = commas+1;	cout<<"ncols selected: "<<ncols<<endl;		istrstream  STRING(ans);	//instantiate instrstream		if ( STRING.fail() ) nrerror("string stream failed");		for (j=1; j<= ncols; j++)			{			STRING >> value;			colsel[j]= (int)value;			cout<<colsel[j]<<"\t";			}		/*------------------------------------------------------------------*/			