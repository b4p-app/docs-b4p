{ -- recodes Statistica MD  ==>>  -999 --- }
Sequential;
for i := 2 to NVars do
begin;
	if not Valid( v(i) ) then 
		v(i) := -999
	else if ( v(i) > 0 ) then
		v(i) :=  ( v(i)*10./10. )
	undefined		
		v(i) := -999 ;
end;
